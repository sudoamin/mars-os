#include <include/console.h>
#include <include/string.h>
#include <stdint.h>

void main(void) {
  // char c = "C";
  // printf("%x\n", &c);

  // should an error be accoured
  // char *r = (char*)(0x400000+(sizeof(main)))-2;

  // char *r = (char*)(0x400000+(sizeof(main)));
  // *r = 10;
  // printf("%u\n", *r);

  // char *b = (char*)(0x400000+(sizeof(main)));
  // printf("%u\n", *b);

  // char *r = (char*)(0x400000+(sizeof(main)));
  // *(r+0) = 97;
  // *(r+1) = 109;
  // printf(*(r+1));
  // printf((char)*(r+1));

  printf("Welcome to MarsOS \n\n");

  while (1) {
  }
}